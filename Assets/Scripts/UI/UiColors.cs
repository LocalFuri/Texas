using UnityEngine;

namespace TexasHoldem
{
    /// <summary>Shared UI gold — matches PotText label (1, 0.85, 0.2).</summary>
    public static class UiColors
    {
        public static readonly Color PotGold     = new Color(1f, 0.85f, 0.2f, 1f);
        public static readonly Color PotGoldDark = new Color(0.65f, 0.55f, 0.13f, 1f);
    }
}
